import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'ionic.changas',
  appName: 'changas',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
